export enum Inputs {
  Name = 'name',
  NamePrefix = 'name-prefix',
  Path = 'path',
  GitHubToken = 'github-token',
  Repository = 'repository',
  RunID = 'run-id'
}

export enum Outputs {
  DownloadPath = 'download-path'
}
